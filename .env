# Build-time feature gates are off unless set to "on". Override locally in
# .env.local, and in CI with the repository variables (.github/workflows/deploy.yml).
# index.html reads VITE_LIGHT_THEME, and Vite warns about an HTML variable
# with no value, so it is given one here.
VITE_LIGHT_THEME=off
